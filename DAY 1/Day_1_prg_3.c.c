#include<stdio.h>
int main()
{
float num;
printf("Enter the radius of the gardan ");
scanf("%f",&num);
//to finding the radiusof garden to use data type as float.
printf("Area is %f",0.314*num*num);
return 0;
}
